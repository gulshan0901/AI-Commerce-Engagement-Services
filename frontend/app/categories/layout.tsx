/** Supplies search metadata for catalogue category routes. */
import type { Metadata } from "next";

export const metadata: Metadata = { title: "Product Categories", description: "Explore the ACE product catalogue by category and find products that match your needs." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
