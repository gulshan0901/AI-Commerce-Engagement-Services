/** Supplies search metadata for the public About route. */
import type { Metadata } from "next";

export const metadata: Metadata = { title: "About", description: "Learn about ACE, an agentic AI commerce platform built for transparent shopping experiences." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
